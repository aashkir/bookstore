/* checking if user already is registered in the DB (not $<var> = variable from code) */
SELECT id FROM user WHERE username = '$username'

/* add the user as the bookstore owner */

/* add the user as a customer */

